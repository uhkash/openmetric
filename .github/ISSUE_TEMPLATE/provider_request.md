---
name: Provider or price update
about: Add a provider, or correct a model price
labels: catalog
---

**Provider slug**

**Base URL**

**Auth style** (bearer / header / query / basic — and the header name if relevant)

**Pricing page** (link, so the numbers can be verified)

**Usage response shape** — paste a redacted example of the `usage` object the API returns,
if it has one:
```json
```

<!-- Most of these are a one-line change to src/openmetric/data/catalog.yaml.
     A pull request is very welcome. -->
